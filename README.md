## Running experiments

Run the baseline evaluation from the repo root:

```bash
python -m experiments.run_eval
```

Each run appends metrics and configuration to `results/experiments.csv` (the directory is created automatically). Use this log to track iterations over time.
